import React from "react";
import './Title.css'

const Title = ({text}) => {
    return(
        <div className='titulo-login'>
           <label className='1'> {text} </label> 
        </div>
    )
};

export default Title; 
